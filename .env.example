# Copy to .env.local for local overrides (optional)

# Backend API base URL (no trailing slash)
VITE_API_BASE_URL=https://www.cgpisoftware.com/cheerytail

# Public URL where this admin panel is hosted
VITE_APP_URL=http://localhost:5173

# Production values (see .env.production):
# VITE_API_BASE_URL=https://www.cheerytails.com/cheerytail
# VITE_APP_URL=https://admin.cheerytails.com
